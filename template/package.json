{
  "name": "{{basename}}",
  "version": "0.0.0",
  "description": "{{{description}}}",
  "repository": {
    "type": "git",
    "url": "https://github.com/nknapp/{{basename}}.git"
  },
  "author": {
    "name": "Nils Knappmeier",
    "email": "npm@knappi.org",
    "url": "https://github.com/nknapp"
  },
  "bugs": {
    "url": "https://github.com/nknapp/{{basename}}/issues"
  },
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "pretest": "standard --version || npm -g install standard",
    "test": "mocha && standard",
    "preformat": "standard --version || npm -g install standard",
    "format": "standard --format"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.2.0",
    "mocha": "^2.2.5"
  },
  "standard": {
    "ignore": [
      "spec/fixtures/**"
    ]
  },
  "keywords": []
}
